# AGENTS.md — 이 리포에서 일하는 코덱스/에이전트 규약 (자동 로드)

> 코덱스는 세션 시작 시 이 파일을 자동으로 읽는다. 그러니 매번 붙여넣지 않아도 규약이 적용된다.
> **아무 작업 전, 아래 두 문서를 반드시 먼저 읽어라(내 철학·우선순위·소통규약·제작규격·지식파이프라인 전부 여기 있다):**
>
> 1. `CLAUDE.md` (리포 루트)
> 2. `docs/코덱스_온보딩_마스터_2026-08-02.md`

## 시작하면 바로
```bash
python3 -m pipeline check                     # 연결 진단(키·인덱스·TTS/Drive/YT)
python3 -m unittest discover -s tests         # 코드 무결성
bash scripts/setup_drive.sh                   # 구글드라이브(플러그인 없이) 세팅·진단
```
읽고 나서 **"온보딩 완료 — 진행중 업무 3줄 요약"** 부터 보고한다.

## 반드시 지킬 핵심 (요약 — 상세는 위 두 문서)
- **디렉터로 일한다**: 기존 자산(리포·드라이브·노션) 검색·재사용 먼저, 중복 생성 금지.
- **소통 3규약**: ①이미 정해진 건 다시 묻지 않는다(`knowledge/결정사항_대장.md`) ②할 수 있는 건 실행하고 결과만 보고 ③사용자 요청은 딱 3종(발행 승인·키 발급·본진 전달 한 줄).
- **콘텐츠 낼 때 2표+제안**: 제작분석 표 + 잘되는콘텐츠 피드백 표 + 더 나은 제안 1픽.
- **글쓰기**: `knowledge/차노_보이스.md` 통과. 남 비판·자기자랑 금지.
- **제작 규격은 코드가 강제**: 렌더는 `shorts/proof.py`(FAIL이면 산출 거부). 발행은 형 승인 후에만.
- **협업**: 착수 전 `git pull --rebase origin main` + `상황판.md` 확인. 커밋마다 pull-rebase 후 push. 예약/자동화·아침 브리핑은 본진 전담(여기서 생성 금지).
- **secrets/ 는 절대 커밋·출력 금지.**

## 🧾 되쓰기(영수증) — 실행하면 반드시 리포에 되쓴다 (2026-08-04 · 손발 연결의 절반)
> 왜: 코덱스가 렌더·업로드·예약을 **로컬/유튜브에서만** 하면 그 사실이 리포에 안 남아 → 클로드(뇌)·본진이 **깜깜**해진다(실증: 다운펌 v5 예약을 클로드가 못 봄). 주문서(매니페스트)에는 **영수증**이 따라야 파이프라인이 닫힌다.
- **산출물을 만들면**: 산출물·매니페스트를 `~/atnown-repo`에 정리하고 커밋·push(세션 임시폴더에 두지 않는다 = 사라짐 방지).
- **업로드/예약/발행을 하면**: 그 즉시 **되쓴다** → ①`knowledge/유튜브_예약현황.md`에 제목·예약시각·영상ID·매니페스트 경로 추가 ②해당 매니페스트에 `"status":"scheduled"`(또는 `rendered`/`published`)·`link` 기록 ③커밋·push.
- **B롤을 새로 올리면**: `knowledge/broll_catalog.json`에 클립 등록(카테고리·file_id·note) 후 커밋 → 재사용·다음 기획에서 보이게.
- 본진 전달분은 드라이브 「앳나운_영상」 브리지 + (정본 상태는) 노션 허브에 반영. **한 일은 항상 흔적을 남긴다.**

*개정은 이찬호 지시로만.*
