# 🔗 AT NOWN 전체 연동 파이프라인 (2026-07-24 이찬호 "다 연결해 파이프라인이야")

> 모든 노드가 하나로 도는 지도. 중심축 4개(레포·디스코드·드라이브·노션)로 노드 7개를 묶는다.
> "연동이 제일 먼저" — 이 지도가 정본. 새 세션/방은 여기부터.

## 노드 7
1. **핸드폰(형=리모컨)** — 어디서든 디스코드/클로드앱으로 지시
2. **Creator OS(맥, 디스코드 봇·영상공장)** — 2026-07-24 재인증 복구(headless OAuth 갱신)
3. **클로드코드 A·B·C방(클라우드 세션)** — 깃 레포 main 공유, 방 자동동기화
4. **맥스튜디오 본진(Creator OS 호스트·드라이브 접근)**
5. **맥북(형 기기)**
6. **옵시디언(형 날것 글, iCloud→드라이브 미러)**
7. **노션(정본 상태판·허브, 형 폰 열람)**

## 중심축 4 (노드를 잇는 버스)
| 축 | 역할 | 상태 |
|---|---|---|
| **깃 레포 `cksghrj22-bot/-`** | 코드·대본·지식·규격 **정본** | ✅ A·B·C방 공유(방 자동동기화: 작업 전 pull, 커밋마다 pull+push) |
| **디스코드** | **실시간 지시 버스**(핸드폰↔Creator OS↔코드방) | 🔶 부분 — 아래 매트릭스 |
| **구글드라이브** | **파일 브리지**(footage·영상·산출물) | ✅ 본진·A방 / ❌ B방 이 세션(드라이브 MCP 없음) |
| **노션** | **정본 상태판·허브** | ✅ MCP 읽기(본진 자동화 페이지 수정 금지) |

## 연결 매트릭스 (from → to : 상태)
| 링크 | 상태 | 비고 |
|---|---|---|
| 핸드폰 → 디스코드 → Creator OS | ✅ | 형 폰 지시 → Creator OS 수신·처리 |
| **코드방B → 디스코드** | ✅ **완료(2026-07-24)** | `shorts/discord_notify.py` 웹훅(UA헤더 필수·없으면403), 204 성공. 내가 상위서 Creator OS에 지시 |
| **디스코드 → 코드방B (수신)** | 🔶 **대기: 봇토큰** | 웹훅은 발송전용. 형 폰 메시지를 B방이 받으려면 디스코드 봇토큰(읽기)+채널ID 필요(형 "전용 봇 신규" 선택) |
| A·B·C방 ↔ 레포 main | ✅ | 방 자동동기화 규약(prompts/방_자동동기화) |
| 코드방 ↔ 드라이브 | ✅A방 / ❌B방(이세션) | B방 파일 브리지는 A방/본진 경유 or 형 채팅 첨부 |
| 옵시디언 → 지식인덱스 | ✅ | 드라이브 미러 → `pipeline add-vault` |
| 노션 ↔ 코드방 | ✅ **읽기·쓰기 available**(2026-07-24 확인, 이찬호 워크스페이스) | fetch/search/create/update OK. 본진 자동화 페이지는 수정 금지 |
| Creator OS ↔ 레포 | 🔶 | Creator OS가 레포를 직접 읽으면 대본·규격 자동참조 가능(본진 설정 여지) |

## 남은 연결(형 조치 1건)
- **디스코드 → 코드방B 수신** = 디스코드 봇토큰 + 채널ID. 주면 `shorts/discord_listen.py`(Monitor 폴링)로 형 폰 메시지 실시간 수신 → 완전 양방향. ⚠️Creator OS 봇과 같은 토큰 두 곳 로그인 불가 → **전용 봇 신규** 또는 전용 채널.

## 파이프라인 흐름(정상 가동 시)
형(폰) →[디스코드]→ Creator OS/코드방 → 대본·규격[레포] → 영상제작[드라이브·render_lib] → 산출[드라이브] → 정본기록[노션·상황판] → 형 확인[노션/디스코드]
