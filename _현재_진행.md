# 🎯 현재 진행 — 라이브 배턴 (새 방은 여기부터 읽고 즉시 이어받는다)

> **방(코워크/코드방/본진)이 멈춰도 이 파일 하나로 다음 방이 바로 이어간다.**
> 규칙: **작업 단위마다 이 파일을 갱신하고 커밋·push**(되쓰기). 이게 최신이 아니면 이어받기가 깨진다.
> 최신 갱신: 2026-08-19

## 📍 지금 상태 (무엇을 어디까지)
- **만화연재 ④ 악성곱슬 통합 재구성 시안 완료**: 구 ⑤고데기를 흡수해 6컷으로 재구성. c2 그림 재사용+문구 교체, c1·c3·c4·c5·c6 재생성. c3 축모/파상모/직파모 라벨은 조립 단계에서 추가. `_out/결이악성곱슬_final__M/` 6장 전부 1080×1350, 본문 밴드 `(250,244,232)` 확인. **미발행·차노 승인 대기.**
- **만화연재 ①편 레퍼런스 복구**: `c1_표지.png`·`c2_매듭.png`·`c6_공간.png` 3장 복구·검증 완료. 이후 만화 파이프라인이 캐릭터·화풍 레퍼런스를 정상 로드할 수 있음.
- **쇼츠 중복·싱크 게이트**: `scripts/shorts_gate.py`에 **S8 최우선→S7→S9** 구현·테스트 완료. 기존 쇼츠 27편 소급은 S8 0/27(로컬 Whisper 없음), S7 픽셀 24통과·2경고·1탈락, S7 매니페스트/S9 0/27(근거 없음). senior 2종·ai일기 1종 인서트 부족으로 새 렌더 중단. 상세 `knowledge/쇼츠_S8_S7_S9_소급검사_20260817.md`.
- **만화연재방 고데기 편 ⑤ 재작업 완료**: c1·c3·c6 재생성, c2 빈 박스 제거, 전 6컷 웜 크림 재조립 완료. `_out/결이고데기_final__M/c1.png`~`c6.png` 모두 1080×1350. c2~c6 밴드 RGB `(250,244,232)` 실측. **썸네일 컷 선택만 차노 대기**, 미발행.
- **AI일기 2편**: 1차 테이크는 차노 판정 "어색함"으로 폐기. 재촬영본 대기. 재촬영본 원클릭 빌더 `scripts/diary_shorts_build.py` 구현·회귀검증 완료.
- **부분펌4**: v8 완성. **코워크 버전이 keeper**(형 판정 2026-08-06 "코워크 게 더 낫다"). 발행 대기(감독 게이트). 위치: `~/atnown-content-pipeline/_jobs/_done/`.
- **개츠비펌 여름시스템**: 렌더 지시 발송됨(디스코드). 렌더 완료 여부 **미확인** → 다음 방 확인 대상. 대본: `_cowork_sync/content/shorts/2026-07-15/01_개츠비펌_여름시스템.txt`.
- **폴더 통합(트렁크)**: 목표=맥 작업폴더 하나로(`~/atnown-content-pipeline` 유력) → 방마다 갈라지는 최신화 방지 + 방 멈춰도 이어받기. **P1 인벤토리 디스코드 발송 완료, 본진 결과 대기 중.** 계획: `knowledge/폴더_통합_계획.md`.

## ▶️ 다음 방이 할 것 (순서)
1. **AI일기 2편 재촬영본이 들어오면** `python3 scripts/diary_shorts_build.py <인터뷰.mov> _clips_pool/ai일기/화면_기록_926MB.mov _out/ai일기/2편_시안.mp4` 실행 → 게이트 시트 보고. 발행 금지.
2. **폴더통합 P1 결과** 확인(본진 채널 보고) → 정리해 형에게 보고 → **P2 유실방지는 형 명령 대기**.
3. **개츠비 렌더 상태** 확인(_jobs/_done에 *개츠비*.mp4 있나).
4. **발행은 형 승인 후에만.** 감독 프리뷰 게이트 유지.

## 🚪 이어받기 진입 절차 (새 방 필독)
1. 정본 폴더에서 `git pull --rebase origin main`
2. `CLAUDE.md` · `AGENTS.md` · `knowledge/결정사항_대장.md` 읽기
3. **이 파일 + `knowledge/방_이어받기_핸드오프.md`** 읽고 위 「다음 방이 할 것」부터 이어감
4. 작업 단위 끝낼 때마다 **이 파일 갱신 + 커밋·push**(되쓰기) — 다음 방을 위해

## 🧭 연결구조 (헷갈리면)
본진 하나·길 셋(코워크=기본 창구/디스코드=입구/코드방=실행층·24-7). 제작=코워크(메인), 조율·감시·통합지휘=코드방. 상세: `knowledge/결정사항_대장.md`.
