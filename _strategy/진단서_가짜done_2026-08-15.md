# 진단서: 가짜 done 사고 (2026-08-15)

## 증상
- 태스크가 `_done/`으로 이동됨
- `status: no_command`, `codex_result: 0 chars`, `completed_at: 없음`
- 게이트/렌더 안 돌았는데 "완료"로 보고됨

## 원인
**두 워처 충돌:**

| 워처 | 역할 | 문제 |
|---|---|---|
| `task_executor.py` | bash 명령 직접 실행 | request에서 ```bash``` 찾음 → 없으면 `no_command`로 _done 이동 |
| `terminal_watcher.py` | Codex API 호출 | 파일이 이미 사라져서 처리할 게 없음 |

**흐름:**
```
태스크 도착 → task_executor가 먼저 가져감 
→ bash 명령 없음 (기획 텍스트라서)
→ no_command로 _done 이동
→ terminal_watcher는 볼 파일 없음
→ Codex 호출 안 됨
→ 게이트/렌더 실행 안 됨
```

## 수정 (2026-08-15 적용됨)
`scripts/task_executor.py` 수정:
- bash 명령 없으면 **파일 이동 안 함** (스킵)
- → `terminal_watcher.py`가 Codex로 처리

**커밋:** e2d7e9d

## 적용 조건
- 본진이 `git pull` 해야 함
- 워처 재시작 필요

## 재발 방지
| 태스크 유형 | 처리 워처 |
|---|---|
| ```bash``` 블록 포함 | task_executor.py |
| 기획/지시 텍스트 | terminal_watcher.py (Codex) |

## 검증 방법
1. 태스크 발주
2. `_done/` 확인
3. `codex_result` 있고 `completed_at` 있으면 정상
