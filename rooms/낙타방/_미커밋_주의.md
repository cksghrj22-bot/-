# ⚠️ 이 폴더는 아직 커밋되지 않았습니다 (2026-08-15 12:10)

`.git/index.lock` 과 `.git/HEAD.lock` 이 남아 있고, 이 방 환경에서는 파일 삭제가 안 돼
`git commit` / `git checkout` 이 전부 막힙니다.

**본진(또는 형 컴퓨터)에서 한 줄만 실행하면 풀립니다:**
```
cd ~/atnown-content-pipeline && rm -f .git/index.lock .git/HEAD.lock
```
그 다음 이 방이 커밋합니다. 그때까지 이 폴더 내용은 디스크에만 있습니다.
(어제 이미 한 번 같은 이유로 작업이 날아갔습니다 — `film_grade.py`, 자막 3체계 정본, shorts_config grades 등.)
