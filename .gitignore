__pycache__/
*.pyc
data/
.venv/
secrets/

# 세션 임시 작업폴더 (기기인증 코드 등 — 절대 커밋 금지)
scratchpad/

# 렌더 산출물(시안 영상) — 드라이브로 업로드, git엔 안 넣음
out_batch_*/
out/
# 렌더 중간산출물(base.mp4·narr·ass 등) — 매니페스트 빌드 캐시, 커밋 안 함
_build/
_video_out/

# 렌더 트리거 로그(코덱스 최종보고 캐시) — 커밋 안 함
_render_trigger_logs/

# outputs/ 렌더 잡 산출물 — 무거운 중간물/최종물은 드라이브行, git 제외.
# (감독 프리뷰 contact/ 시트만 추적 유지 → 아래에서 제외 안 함)
outputs/*/broll/
outputs/*/vendor/
outputs/*/work/
outputs/*/final/
outputs/*/fonts/
outputs/*/bin/
outputs/*/render_config.json
outputs/_backup/

# 로그 파일 (git 제외)
logs/

# 임시/리뷰 파일 (git 제외)
_tmp/
_jobs/_done/_review/
_out/capsheet/

# 대용량 소스 — 커밋 금지 (CLAUDE.md 규칙)
_clips_pool/
_naver_profile/
_tmp/
