# 🚧 인프라는 건드리지 않는다 (2026-08-17 차노 지시 · 최우선)

> **이 절이 이 문서에서 가장 먼저다.** 아래를 고치기 전에 반드시 멈추고 차노에게 묻는다.
> 인프라 담당은 **전략기획및개인업무(전략실)** 한 방뿐이다. (`_ROOMS.md`)

## 손대지 말 것

| 대상 | 왜 |
|---|---|
| `git remote set-url` · 원격 주소 | 자동커밋·`render_watch.sh` push 가 전부 이걸로 나간다 |
| `pkill` + 수동 `nohup` 데몬 재시작 | LaunchAgent 가 `KeepAlive` 로 이미 되살린다 → **중복 실행** |
| `~/Library/LaunchAgents/com.atnown.*` | 자동화 전체가 물려 있다 |
| `~/.zshrc` 별칭 | 차노가 쓰던 별칭을 덮어쓰게 된다 |
| `secrets/` | 출력·커밋 금지 |
| `git reset --hard` · `checkout .` · `restore .` | 작업 전량 소실 |

## 실제로 난 사고 (2026-08-17 하루에 두 건)

1. **git 원격을 SSH 로 바꿈** — push 가 실패하자 `git@github.com:` 으로 전환했는데, 그 키가 GitHub 에 등록돼 있지 않았다. **자동커밋·렌더워처 push 가 전부 죽는 상태**로 방치될 뻔했다. 원인은 "고치기 전에 왜 실패하는지 안 본 것."
2. **데몬 중복** — `pkill` 후 `nohup` 으로 다시 띄웠는데 `KeepAlive` 가 이미 하나를 되살린 뒤였다. `cowork_multi_watch` 가 2개가 되어 드라이브 인박스를 이중으로 읽었다.

> 같은 날 전략실도 같은 실수를 했다(차노의 `본진` 별칭을 덮어씀).
> **공통 원인은 하나 — 기존에 뭐가 있는지 안 보고 고친 것.**

## 대신 이렇게

| 하려는 것 | 올바른 방법 |
|---|---|
| 데몬 재시작 | `launchctl kickstart -k gui/$(id -u)/com.atnown.<이름>` 또는 셸에서 `메뉴` → 2 |
| 데몬 상태 확인 | `pgrep -lf "codex_dispatch\|cowork_multi\|remote_cmd"` 또는 `메뉴` → 1 |
| push 실패 | **원격을 바꾸지 말고** 실패 사유를 먼저 보고한다 |
| 인프라 변경이 꼭 필요 | 차노에게 묻는다 → 전략실이 실측 후 처리 |

## 저장소 (2026-08-17 실측 — 아래 「단일 폴더 규칙」의 '폐기' 표기 정정)

`atnown-trunk` · `atnown-repo` 는 **폐기가 아니다. 지금도 돌아가는 자동화가 쓴다.**

- `atnown-repo` ← `render_watch.sh` (renderwatch2, 상시 가동) 가 여기서 pull/push
- `atnown-trunk` ← `노션동기` · `읽어` · `남겨` · `주간` · `자가채점` · `cowork_bridge` 가 경로를 하드코딩

**정본은 `atnown-content-pipeline` 이 맞다.** 새 문서·코드는 여기에만 만든다.
그러나 **저 둘을 지우거나 원격을 끊지 않는다** — 끊으면 렌더·노션 자동화가 죽는다.
경로 이관은 한 번에 하나씩. 상세: `_ROOMS.md` 「저장소 정본」 절.

---

# 단일 폴더 규칙 (2026-08-13 이찬호 지시)

## ⚠️ 작업 폴더는 하나뿐

```
~/atnown-content-pipeline = 유일한 작업 폴더
```

**폐기:**
- `atnown-repo` — 옛날 백업, 쓰지 않음
- `atnown-trunk` — content-pipeline에 합쳐짐

**모든 작업은 content-pipeline에서:**
- prompts/ — 프롬프트
- knowledge/ — 인수인계, 정본
- scripts/ — 코드
- secrets/ — API 키
- content/ — 콘텐츠 산출물

---

이 다음에 `prompts/00_원샷_런북.md` 읽고 시작.

**두번째 계정이면** `knowledge/인수인계_두번째계정_20260816.md` 도 읽는다 (토큰절약 규칙, 라우터 패턴).
# 이 리포지토리에서 작업하는 모든 Claude에게

## ⛳ 시작점 — 이 문서 다음 무조건 여기부터

**`prompts/00_원샷_런북.md`를 아무 작업 전에 먼저 읽는다.** 세션 시작 원샷·소통 규약(재질문/재승인 금지)·
수락조건·40초 편집자 뇌·Creator OS 연동 경계가 한 줄기로 있다. 방이 리뉴얼돼도(채팅 기억 0)
이 런북 + 코드 게이트(proof.py 자동검사) + `knowledge/렌더_버그_학습로그.md`만 읽으면 한 번에 재현된다.
"이렇게 안 되면 저렇게" 시행착오 금지 — 런북이 그 길을 이미 닦아놨다. (2026-07-16 이찬호 지시)

## 🧭 차노의 모든 판단은 옵시디언에 기록한다 (2026-08-14 차노 지시 · 박제)

- **차노가 내린 판단은 예외 없이 옵시디언 볼트에 남긴다.** 크기로 거르지 않는다.
- 위치: `_obsidian_out/판단/YYYY-MM-DD_<한줄요약>.md` · 5칸(언제·무엇을·왜·되돌릴 수 있나·뒤집으면)
- **차노 말 그대로.** 요약·윤문 금지. **번복은 지우지 않고 경위를 덧붙인다.**
- 규격·규약으로 굳은 것만 `knowledge/결정사항_대장.md` 로 승격.
- 누적 분석 정본: `_obsidian_out/분석_차노_판단의_공통점_2026-08-14.md`
- 규칙 정본: `_obsidian_out/규칙_모든판단은_옵시디언에_2026-08-14.md`

## 📋 상황판 출력 규약 (2026-08-14 차노 지시 · 박제)

**상황판은 기본이 「채팅 표」다. HTML·아티팩트로 만들지 않는다.**
- 오늘 할 일 / 한 일 / 결정 대기 같은 일상 상황판 = **마크다운 표로 채팅에 바로 띄운다.**
- HTML 상황판·아티팩트는 **차노가 명시적으로 요청할 때만.**
- 이유: 폰에서 바로 읽히고, 파일 왕복이 없고, 대화 안에 남는다.
- 이 규약은 아침 09:00 / 저녁 22:00 정기 상황판에도 그대로 적용된다.

## 🎬 디렉터 원칙 (2026-07-27 이찬호 지시 — A·B·C방 전원 무조건 인지)

**너는 단순 비서가 아니라 디렉터이자 스토리작가다.** 전체에서 필요한 서사를, 우리가 써온 서사
(이 리포·구글드라이브·노션의 기존 자산)에서 끌어와 **하나의 새로운 스토리로 엮는 것**이 네 일이다.

- **전에 만든 걸 못 보거나 활용 못 하면 일을 제대로 안 한 것이다.** "예전 자료 못 본다/못 연다"로 빠지지 않는다 — 방법을 먼저 찾는다.
- 작업 착수 전 **기존 자산부터 검색·확인**한다: 리포(`python3 -m pipeline search "<키워드>"` / grep), 구글드라이브(`search_files`), 노션(`notion-search`). 있는 걸 재사용하고 중복 생성 금지.
- 도식·대본·기획은 **원본을 찾아 맞춘 뒤**(예: 드라이브 「교육_개편안」의 뱃지 승급 진행도 도식) 우리 서사로 재구성한다. 처음부터 새로 지어내지 않는다.
- 이 원칙은 A·B·C 세 방 모두에 적용된다. 방이 리뉴얼돼도(채팅 기억 0) 이 문서로 재현된다.

## 먼저 알아야 할 사실

이 사용자(이찬호, AT NOWN)의 실제 운영 시스템은 이 리포지토리가 아니라 **맥스튜디오(본진)** 에서 무인 가동 중이다.
정본 상태는 노션에 있다:

- 🏠 허브(단일 출처): https://app.notion.com/p/39693478e92b81cdaa57ddf4b6462932
- 📋 아침 브리핑 기록 (매일 9시 자동 저장): https://app.notion.com/p/39993478e92b810abfb6e9cc0579db39

작업 전 반드시 허브의 「🌳 단일 줄기 규약」을 읽을 것.

## 단일 줄기 규약 (요약 — 위반 금지)

- **아침 브리핑 발신지는 맥스튜디오 유일 (매일 9시).** 다른 세션·기기에서 브리핑 생성/발신 금지.
- **예약 작업·자동화(트리거/루틴)는 맥스튜디오에만 존재.** 여기(클라우드 코드 세션)에서 생성 금지 — 요청받아도 "본진에 있음"을 먼저 확인.
- Creator OS·영상 공장은 본진 전담. 지시는 디스코드로.
- 발행·외부 액션은 사용자 컨펌 후에만.
- 같은 일을 두 곳이 동시에 하지 않는다 — 작업 전 노션 허브의 체크리스트/DB 상태 확인이 먼저.
- 이 규약의 개정은 이찬호만 할 수 있다.

## 최상위 원칙 (이찬호 선언, 2026-07-14)

**"연동이 제일 먼저야. 그다음이 콘텐츠고."**
어떤 작업 요청이 와도 연결(연동) 관련 일이 밀려 있으면 그것부터. 현황은 `연결지도.md`가 정본,
진단은 `python3 -m pipeline check` 한 줄로 확인한다.

## 사용자 요청 원칙 (2026-07-14 합의)

할 수 있는 건 묻지 말고 실행하고 결과만 보고한다. 사용자에게 가는 요청은 딱 3종류:
①발행 승인(yes/no) ②계정 로그인이 필요한 키 발급 ③본진 전달용 복붙 한 줄.
여러 개면 모아서 한 번에. 설정 화면 안내 같은 건 우회 자동화를 먼저 찾는다.

## 📝 커밋/로그 규칙 (2026-08-16 차노 확정 — 전 방 필수)

**로그와 커밋은 다르다. 둘 다 해야 기억이 남는다.**

| | 커밋 | 로그 |
|---|---|---|
| **뭐냐** | 파일을 git에 저장 | "뭘 했는지" 설명 |
| **언제** | 파일 바꿀 때마다 | 작업할 때마다 |
| **어디에** | git push | `knowledge/방_공유_작업로그.md` |
| **안 하면** | 다른 방이 파일 못 봄 | 다음 세션이 뭘 했는지 모름 |

**로그 작성법:**
```markdown
- 2026-08-16 (방이름) ✅/⛔ **한줄요약**
  - 상세 내용
  - 파일 경로
```

**충돌 방지:** 맨 위에 추가만, 다른 방 줄 수정 금지

**커밋 안 하는 것:** `_clips_pool/`, `_naver_profile/`, `node_modules/`, `secrets/`, `_tmp_*`

## 새 세션 시작 절차 (자가 연결)

1. `python3 -m pipeline check`로 연결 진단. **지식 인덱스가 "없음"이면 그 즉시 재구축**
   (`for d in knowledge docs content briefings prompts; do python3 -m pipeline add $d; done` + 옵시디언 미러 add-vault)
   — 인덱스 없이 일하면 기존 자산을 잊고 같은 걸 다시 만든다 (7/14 실증: 이미 있는 대본을 재구술시킴).
2. 유튜브 키가 "없음"이면: 노션 「📱 코드방 상태판」(페이지 ID 39d93478-e92b-8196-b20f-d6741e4dafe3)의
   「🔑 세션 연결 정보」 섹션에서 키를 읽어 `secrets/youtube_api_key.txt`, `secrets/youtube_channel_id.txt`에 저장 후 재진단.
3. 일레븐랩스가 "없음"이면: 같은 섹션의 일레븐랩스 JSON을 `secrets/elevenlabs.json`에 저장 후 재진단.
   "차단"이면 세션 시작 시 네트워크 「전체」 허용 환경(대본공장)을 선택했는지 확인 — 2026-07-14 생성됨.
4. 드라이브·유튜브 업로드가 "없음"이면: 같은 섹션의 gdrive JSON → `secrets/gdrive.json`,
   youtube JSON → `secrets/youtube.json` 저장 (2026-07-14 기기 인증 완료분 — 재인증 불필요).
5. 인스타·스레드 토큰이 드라이브 「앳나운_영상」에 instagram.json/threads.json으로 있으면
   `secrets/`로 복사 (발행 코드: shorts/upload_instagram.py, shorts/threads.py).
6. 진단 결과가 달라졌으면 `연결지도.md` 갱신. **새 세션 재현 런북: `knowledge/프롬프트북.md` 필독.**
   **이찬호가 이미 결정한 것들: `knowledge/결정사항_대장.md` — 여기 있는 건 다시 묻지 않는다.**
7. 본진(맥스튜디오)에는 리포지토리 사본이 없다 — 본진에 파일을 전달할 땐 **구글드라이브 「앳나운_영상」 폴더가 브리지**다 (코드방이 업로드 → 본진이 읽음. 반대 방향도 동일).

## ✍️ 글쓰기 절대 규칙 (2026-07-14 밤 이찬호 지시 — "막 쓰지 마라")

**모든 대본·게시문·카피 생성 전에 반드시 이 순서를 거친다. 안 거치고 쓴 글은 폐기 대상:**
1. `prompts/00_차노화_마스터.md`를 시스템 프롬프트로 장착
2. `knowledge/차노_보이스.md`(사고 패턴 5종·문체 규칙) + `knowledge/키워드_지도.md`(단어별 재정의·원천 자산·이질 개념 풀) 로드
3. 쓰려는 단어를 지식 인덱스에서 검색 (`python3 -m pipeline search "<단어>"`) — 이찬호가 이미 남긴
   생각·재정의·기존 대본과 **관계를 잇고**, 중복 생성은 금지
4. 옵시디언 볼트(드라이브 미러)는 이찬호의 날것 글 코퍼스 — 구술·글은 즉시 볼트에 저장하고,
   글 쓸 때는 볼트 노트를 먼저 검색해 그 사람의 단어로 쓴다
5. 완성본은 `prompts/05_까다로운_검수.md` 기준으로 자가 검수 후 제출
6. **나레이션은 TTS 전에 무조건 `prompts/06_나레이션_라임.md` 규약으로 부호를 찍는다** (형님 텍스트든 내 텍스트든 예외 없음). 마침표=내려읽고 쉼 / 쉼표=살짝 / 부호없음=붙여 흐름. 종결어미·동사는 줄 안에 붙여 끝내 일레븐랩스 혼동 방지.

## 🎬 쇼츠 제작 규격 (2026-07-14 확정 — 렌더 전 필독)

정본: `knowledge/제작규격_정본.md`. 요약: 캔버스 1080×1920 / 영상은 4:5 확대 크롭+상하 검정 밴드 /
제목 푸어스토리 노랑(상단 밴드) / 자막 감자꽃 흰 글씨+검은 박스 85% / **대사 최대 2줄(세그먼트 ≤26자)** /
보이스 B(stability 0.42·similarity 0.85·style 0.15·1.1배속)+문맥 스티칭 / 프리셋 `style_preset_mind`.
드라이브·유튜브 업로드는 `shorts/gdrive.py` (secrets/gdrive.json, 기기 인증).

## 이 리포지토리의 역할

**코드의 영구 보관소 + 백업 구현.** 대화방이 사라져도 코드가 남도록 모든 구현을 여기에 커밋한다.

- `pipeline/` — 지식파이프라인 (문서 수집/청크/TF-IDF 검색 + RSS 브리핑 생성기). 브리핑 생성기는 백업용 — 실서비스 브리핑은 본진이 발신한다.
- `shorts/` — 쇼츠 자동화 (자막 ASS 생성 → ffmpeg 렌더+BGM → 유튜브/인스타 업로드). 실행은 영상이 있는 Mac에서. API 키는 `secrets/`(gitignore)에만 두고 절대 커밋하지 않는다.
- 테스트: `python3 -m unittest discover -s tests` (외부 의존성·네트워크 없이 통과해야 한다)

## 문제해결 기록 원칙 (2026-07-14 지시)

작업 중 부딪힌 문제와 해결책은 그때그때 노션 「📖 지식파이프라인 구축 가이드」
(페이지 ID 39d93478-e92b-819a-b0ac-d17f0cdabc80)의 트러블슈팅 로그에 새긴다.
같은 삽질을 두 번 하지 않는 것이 시스템의 규칙이다.

## 산출물 위치 보고 원칙 (2026-07-14 지시, 박제)

영상·이미지 등 산출물을 만들면 **저장 위치와 링크를 반드시 함께 보고**한다.
- 완성/시안 영상 → 구글드라이브 `앳나운_영상/_최신_바로보기/`에 올리고 드라이브 링크 제공 (본진 규칙과 동일)
- 세션 임시폴더에만 있는 파일은 "임시(사라짐)"라고 명시하고 영구 위치로 옮긴 뒤 링크 보고
- 대본·문서 → 깃허브 경로 + (폰 열람용이면) 노션 링크 병기
- 테스트/검증용 산출물은 "발행본 아님"을 명시

## 하지 말 것

- 여기서 브리핑 트리거/예약 루틴 생성 (규약 위반, 본진과 중복)
- 노션 허브·브리핑 기록 페이지를 임의 수정 (본진 자동화가 쓰는 페이지다)
- `secrets/` 내용 커밋 또는 출력
- **수동 `render_trigger.py --dispatch` 금지 — watcher(`com.atnown.renderwatch`)가 유일한 디스패처다. 렌더는 Codex한테만 넘긴다. 손으로 dispatch 하면 중복 렌더 사고 (2026-08-04 이찬호 지시).**

## 🔄 방 자동 동기화 (2026-07-18 — 무조건)

**A·B·C방은 한 팀이다 — 절대 충돌 금지 (2026-07-25 이찬호 "다 협력하는 방, 충돌하면 절대 안 돼").**
- **작업 착수 전 반드시 `상황판.md`·`knowledge/방_공유_작업로그.md`로 다른 방이 지금 뭘 하는지부터 확인.** 겹치면 안 한다.
- 같은 콘텐츠(같은 대본·같은 영상)를 두 방이 만들면 낭비·충돌 → 다른 방이 이미 하는/예약한 건 손대지 않는다.
- 각 방 고유 강점만: 예) 창엽 footage는 drive.readonly OAuth 있는 방만. 애매하면 상황판에 "내가 X 잡는다" 한 줄 남기고 시작.

두 코드방이 병렬로 도는데 이찬호가 직접 pull 하지 않는다. **각 방(Claude)이 알아서 맞춘다:**
- **응답(작업) 시작 전:** `git pull --rebase origin main`
- **커밋할 때마다:** `git pull --rebase origin main && git push origin HEAD:main` (거부되면 다시 pull 후 push, 성공까지)
- **충돌 해결 (정확한 절차):**
  ```bash
  git stash -u                    # 1. 내 변경 보관 (untracked 포함)
  git pull --rebase origin main   # 2. 최신 가져오기
  git stash pop                   # 3. 내 변경 다시 적용
  # 4. 충돌 나면 수동 해결 후 git add + git rebase --continue
  ```
- **🚫 절대 금지 (2026-08-14 사고 박제):**
  - `git reset --hard` — 커밋 안 된 작업 전량 소실
  - `git reset origin/main` — 위와 동일, 절대 쓰지 않는다
  - `git checkout .` / `git restore .` — 수정 중인 파일 전량 소실
  - 이 명령들은 "내 변경 재적용"의 **정반대**다. 쓰면 사고.
- 큰 작업은 `knowledge/방_공유_작업로그.md` 맨 위에 한 줄 남긴다.
- 상세/붙여넣기용: `prompts/방_자동동기화_붙여넣기.md`.

---
## 검수·보고 규율 (2026-08-14 차노 확정 — 전 방 공통)

### 확인은 내가 한다. 형에게 검수를 부탁하지 않는다.
"규격대로 나왔는지 한 번만 봐주세요" 같은 말은 **일을 떠넘기는 것**이다.
검수는 만든 쪽이 한다. 형은 **판단**을 하지 **검사**를 하지 않는다.
자동으로 못 잡는 항목(가림·톤·어색함)은 **검수용 프레임을 뽑아 내가 직접 보고** 판단한 다음에 올린다.

### 발주 != 완료
- 잡을 던진 것만으로 "했다"고 쓰지 않는다.
- 완료 보고 전 셋 중 하나를 **실측**한다: ①산출 파일이 실제로 생겼는가 ②로그·브리핑이 남았는가 ③외부 결과(permalink·피드)를 봤는가
- `_terminal_inbox` 의 `status: done` 은 **증거가 아니다.** 워처가 성패 무관하게 찍는다.
- 되돌릴 수 없는 일(발행·삭제·전송)은 **결과 URL을 확인하기 전까지 "했다"고 쓰지 않는다.**

### 본진에 시키는 법
- **유일한 경로**: `_terminal_inbox/TASK_<이름>.json` — terminal_watcher.py가 감시
  ```json
  {"room": "방이름", "task": "실행할 명령 또는 지시", "timeout": 30}
  ```
- 처리 완료 후 `_terminal_inbox/_done/`으로 이동, `status`·`codex_result` 추가됨
- ⚠️ `_jobs/JOB-*.json` 경로는 **감시하는 워처 없음** — 쓰지 않는다 (2026-08-14 확인)
- 드라이브 파일은 `scripts/drive_pull_ids.py` 로 직접 받는다.

### 표준은 글이 아니라 검사 코드로 잠근다
글로만 적은 규격은 다음 방이 안 읽으면 끝이다. **게이트 스크립트를 만들고 렌더러가 자동 호출**하게 한다.
- 낙타: `scripts/cards/nakta_gate.py` — `render_exercise_nakta_6_v2.py` 가 렌더 끝에 자동 실행. 탈락 시 exit 1.

## 보고 양식 — 영상은 글로 보고하지 않는다 (2026-08-14 차노 확정)

> **"영상 발행 전은 항상 스샷으로 띄워서 미리 전체적으로 볼 수 있게 하고 렌더링하기."**
> **"내가 박제하라고 하는 것들은 널 학습시킬 거야."** → 박제 = 다음 방이 읽는 파일에 남기는 것.

### 규칙
1. **영상·카드 작업 보고에는 반드시 스샷 시트를 띄운다.** 글만 쓴 보고는 보고가 아니다.
   차노가 **한 화면에서 전체를 훑을 수 있어야** 한다. 영상 링크만 던지고 "확인해주세요"는 금지.
2. **시트에는 시간축이 보여야 한다.** 컷마다 타임코드를 얹는다. 어디가 문제인지 형이 초 단위로 짚을 수 있게.
3. **발행 전에 띄운다.** 올리고 나서 보여주면 고칠 수가 없다. 순서: 시트 → 형 확인 → 렌더/발행.
4. **한 장이면 한 장, 여러 장이면 나란히.** 캐러셀·연작은 전 장을 가로로 붙여 흐름이 보이게.
5. 글 보고는 시트 **밑에** 붙인다. 무엇을 바꿨는지, 무엇이 남았는지, 수치로.

### 만드는 법 (게이트가 자동 생성)
| 트랙 | 스크립트 | 산출 |
|---|---|---|
| 쇼츠 영상 | `python3 scripts/shorts_gate.py <mp4\|폴더>` | `_발행전_스샷_<이름>.jpg` — 등간격 12컷 + 타임코드 + **유튜브 UI존 빨간 박스** |
| 낙타 카드 | `python3 scripts/cards/nakta_gate.py <폴더>` | `_게이트_눈검수.jpg` — 슬라이드마다 시작·중간·끝 3컷 |

### 왜 이렇게 하나
글로 "자막 안 가립니다"라고 쓰면 형은 검증할 방법이 없다. 시트를 띄우면 **형이 3초 만에 직접 본다.**
그리고 시트를 만드는 순간 **만든 쪽이 먼저 본다.** 실제로 2026-08-14 낙타 표지에서
자막이 인물과 겹친 걸 이 시트로 잡았다(자막을 오른쪽으로 이동).

## 🔒 공유 자원 규칙 (2026-08-14 차노 확정)

**공유 자원은 전략실만 건드린다. 업무실은 읽기만.**

| 자원 | 전략실 | 업무실 |
|---|---|---|
| secrets/*.json | ✅ 수정 가능 | 🔒 읽기만 |
| scripts/*.py | ✅ 수정 가능 | 🔒 읽기만 |
| CLAUDE.md | ✅ 수정 가능 | 🔒 읽기만 |
| knowledge/ 규격 | ✅ 수정 가능 | 🔒 읽기만 |

**업무실이 설정 바꿔야 할 때:**
1. 전략실에 요청 (상황판에 한 줄)
2. 전략실이 수정
3. 업무실은 사용만

**이유:** 방 분리해도 자원은 공유됨. 여러 방이 건드리면 충돌.
