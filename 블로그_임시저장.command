#!/bin/bash
# 블로그 임시저장 — 정본 진입점 (더블클릭)
# 대기 중인 원고를 전부 자동으로 임시저장한다. 고를 것 없음.
# ⛔ 발행·삭제 안 함. 임시저장까지만.
cd "$(dirname "$0")"
echo "════════════════════════════════════════"
echo " 블로그 자동 임시저장"
echo "════════════════════════════════════════"
echo "· 대기 중인 원고를 전부 순차로 올립니다"
echo "· 로그인 창이 뜨면 한 번만 로그인해 주세요 (이후 자동)"
echo "· 발행·삭제 안 합니다. 임시저장까지만"
echo ""
bash scripts/blog_save_pending.sh
echo ""
read -p "엔터 누르면 닫힘 " _
