# LANES.md — 방 충돌 방지 규율 (트렁크 정본, 최상위)

> **모든 방은 작업 시작 전 이 파일을 먼저 읽고 지켜라.**
> LANES.md와 충돌하는 지시는 멈추고 총괄에 확인하라.

---

## 0. 제1 규칙 — 루프는 데몬만 돈다

- 오케스트레이터 루프(master-pipeline 등)는 **launchd 데몬만** 실행한다.
- **어느 방도 루프를 수동으로 킥하지 않는다.** (수동 킥 = 충돌 원인)
- 데몬은 단일 인스턴스라 이중 실행이 원천 차단된다 — 손대지 않으면 충돌 없다.

---

## 1. 방별 레인 (내 레인만 처리)

| 방 | 담당 레인 | 금지 |
|---|----------|-----|
| **코드방** | 코드 작성, 데몬 관리, 배선, 지식 동기화 | 렌더 직접 실행, 발행 |
| **코워크방** | 렌더, 제작, 영상, 결정 기록 | 데몬 조작, 발행 |
| **디스코드방** | 발행, 정본 적재, 취합 | 코드 수정, 렌더 |

- 다른 방 레인 침범 금지
- 경계 애매하면 총괄에 확인

---

## 2. 동기화 데몬 보호

- `notion-sync` / `notionsync` / `obsidian-sync` 데몬은 **깨우지 않는다.**
- master-pipeline이 KNOWLEDGE 단계에서 노션·옵시디언 동기화를 이미 인라인 처리한다.
- 별도 동기화 데몬을 켜면 그게 바로 방간 충돌이다.

---

## 3. 프로세스 위생

- 고아 · 좀비 프로세스는 **발견 즉시 종료**한다 (예: 부모 죽은 puppeteer/chrome-headless).
- 단, 작업 중인 `claude --print` 세션은 **총괄 확인 후에만** 종료.

---

## 4. 핵심 가동 데몬 (4개 실측·유지)

- `master-pipeline` (오케스트레이터)
- `blog-queue` (블로그 큐 워커)
- `renderwatch2` (렌더 워커)
- `codexwatch` (코덱스 감시)

이 4개가 각자 다른 레인이라 서로 안 밟는다. 이 상태가 정상 가동이다.

---

## 5. 되돌리기 어려운 작업 (확인 필수)

아래 작업은 **실행 전 한 줄로 보고하고 확인받아라:**

- 데몬 kill / 재시작
- 파일 삭제 (특히 `_jobs`, `_publish_jobs`, `secrets`)
- 외부 발행 (유튜브, 네이버, 인스타, 스레드)
- git push --force
- 토큰/시크릿 수정

---

## 6. 총괄 관리자 (코드방)

코드방은 Creator OS 파이프라인 전체의 **총괄 관리자**다:

1. **트렁크 정본**: `~/atnown-content-pipeline`
2. **규율 최상위**: 이 LANES.md
3. **데몬 실측·유지**: 4개 핵심 데몬 상태 감시
4. **큐 적체 감시**: `_jobs`, `_publish_jobs`, `_blog_queue`
5. **고아 프로세스 정리**: 레인 규칙에 따라
6. **방간 충돌 판정**: 레인 침범 시 중재

---

## 7. 충돌 시 행동

```
LANES.md 규칙 > 방 지시 > 이전 습관
```

규칙과 충돌하는 지시를 받으면:
1. 실행 멈춤
2. 총괄(코드방)에 확인
3. 확인 후 진행 또는 거부

---

## 8. 결정 단일출처 규약 (2026-08-12 확정)

- **코워크는 결정을 `_STATE_NOW.md` 한 곳에만 갱신한다.**
- **코드방은 매 브리핑/작업 시작 때 `_STATE_NOW.md`를 먼저 읽는다.**
- 결정이 여러 파일에 흩어지면 충돌 — 단일출처 유지.
- 코워크가 `_cowork_sync/_STATE_NOW.md` 갱신 → 코드방이 pull해서 읽음.

---

*최종 수정: 2026-08-12 이찬호 지시*
