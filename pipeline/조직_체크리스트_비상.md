# 앳나운 콘텐츠 조직 · 업무범위 · 체크리스트 · 비상 인계 (2026-08-06 박제)

> 형(감독)은 **씨앗 + yes/no만**. 나머지는 각 유닛이 알아서. 선처리 후보고.

## 1. 조직도 · 업무 범위 (누가 뭘 하나)

| 유닛 | 한 줄 역할 | 하는 것 | 안 하는 것 |
|---|---|---|---|
| 🎬 감독(차노/형) | 방향·판정 | 씨앗 투입, yes/no, 창의 갈림 결정 | 클립 찾기·설정·렌더 (전부 위임) |
| 🧠 Cowork (이 방) | 뇌·디렉터·**렌더까지** | 기획·대본·첨삭·결정 박제·자산 드라이브 자가탐색·ffmpeg 렌더·트렁크 커밋·형 창구 | 대량 밤샘 렌더(본진), 복잡 코드(Codex) |
| 🤖 Codex | 코드 손 | Cowork가 못 짜는 복잡 코드/벌크 작업 (`_codex_inbox` 브리프 수령) | 콘텐츠 판단·발행 |
| 🖥 코드방(Claude Code, 본진) | 밤샘 렌더·발행 엔진 | 나이틀리 대량 렌더, 유튜브 발행, render_watch | 현재 불안정 → **백업 라인** |
| 🎥 Creator OS (본진) | 디스코드 영상봇 | 디스코드 명령 실행, 켄번스 프리미엄 렌더 | 승인 없는 발행 |

**현재 실질 주력 = Cowork(이 방).** 렌더 이 방에서 안정 가동 확인(2026-08-06 개츠비 실렌더). 본진은 자꾸 hang → 백업.

## 2. 유닛별 자체 체크리스트

### 🧠 Cowork
- 착수 전: `git pull --rebase` · `상황판.md`·`_ROOM_HEARTBEAT.md` 읽기 · **자산은 드라이브 search로 내가 찾기(형한테 요청 금지)**
- 대본: 차노 보이스 규약 · 밑줄 제목 규칙 · 재질문 금지
- 렌더: `fc-list`에 교보 확인 · TTS 도달 테스트 · 규격 8항목 PASS · **BGM 아웃트로 −45dB↑(무음 아님)** · 보이스ID=6bJj
- 완료: 산출물 드라이브 업로드+링크 · 커밋 · 하트비트 갱신

### 🤖 Codex
- 브리프 수령 확인 → 실행 → 결과 `_cowork_sync` 회신 → 첫 실행 로그 검증(명령 맞나)

### 🎥 Creator OS
- 디스코드 명령 수신 → 실행 → 결과 회신 → **발행은 감독 승인 후만**

## 3. 브릿지 핸드오프 체크리스트 (정보 넘어갈 때 "완벽" 기준)

| 브릿지 | 넘길 때 반드시 | 받는 쪽 검증 |
|---|---|---|
| Cowork → 트렁크 | 커밋 메시지 명확 · **secrets 미포함 확인** | push 성공(or 막힘 기록) |
| Cowork → Codex (`_codex_inbox`) | 목표·입력·기대출력·검증법·파일경로 | 첫 실행 로그로 명령 맞나 확인 |
| Cowork → Creator OS (디스코드 웹훅) | 명령 1줄 + 대상 드라이브 링크 + 발행여부 | 실행 결과 회신 |
| 트렁크 → 본진 (pull) | 최신 tip 기준 | 충돌 시 최신(다른 방) 살리고 재적용 |
| 산출물 → 드라이브 | `_최신_바로보기` 업로드 · **발행본/시안 표기** | 링크 열림 확인 |
| 결과 → Cowork (회신) | **실제 파일·로그 첨부(주장 아님)** · 오디오/폰트 실측 포함 | Cowork 더블체크 |

**핵심 원칙: "됐다"는 말이 아니라 실측/파일로 증명. 주장만 넘기면 그 브릿지는 실패.**

## 4. 비상 인계 (33시간 갭 → 분 단위)

**원인**: 방이 로컬로만 일하고 push 안 하면 다른 방이 못 봄 + 세션 hang. (2026-08-06 실제 33h 갭 발생)

**시스템**:
1. **하트비트**: 모든 방이 의미있는 스텝마다 `pipeline/_ROOM_HEARTBEAT.md`에 `{방·UTC시각·현재작업·마지막 안전상태·다음 스텝}` 1줄 갱신 + push.
2. **인계 규칙**: 대기 방이 pull → 하트비트가 **20분 이상 stale**이면 "다음 스텝"부터 이어받음. (33h 안 기다림)
3. **Cowork 예외(push 막힘)**: Cowork는 하트비트를 **노션 작업큐(Cowork↔본진 명령버스)에도 미러** → push 없이도 다른 방이 읽음. 근본해결=세션 소스 등록으로 push 열기.
4. **산출물 안전**: 완성 영상은 **즉시 드라이브 업로드**(세션 초기화돼도 남음). 임시폴더에만 두지 않음.

**새 방이 이어받는 3줄 런북**: ① `git pull` + `_ROOM_HEARTBEAT.md`·`상황판.md` 읽기 ② secrets 노션에서 복원(`CLAUDE.md` 자가연결) ③ 하트비트 "다음 스텝"부터 실행.
